import 'package:flutter/material.dart';

class MenPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Men'),
      ),
      body: Center(
        child: Text('Men Page Content'),
      ),
    );
  }
}