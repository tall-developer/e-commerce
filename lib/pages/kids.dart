import 'package:flutter/material.dart';

class KidsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Kids'),
      ),
      body: Center(
        child: Text('Kids Page Content'),
      ),
    );
  }
}