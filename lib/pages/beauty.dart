import 'package:flutter/material.dart';

class BeautyPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Beauty'),
      ),
      body: Center(
        child: Text('Beauty Page Content'),
      ),
    );
  }
}