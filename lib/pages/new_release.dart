import 'package:flutter/material.dart';

class NewReleasePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('New Release'),
      ),
      body: const Center(
        child: Text('New Release Page Content'),
      ),
    );
  }
}
