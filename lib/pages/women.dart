import 'package:flutter/material.dart';

class WomenPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Women'),
      ),
      body: Center(
        child: Text('Women Page Content'),
      ),
    );
  }
}